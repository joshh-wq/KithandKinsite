---
---

<footer class="footer">
  <div class="container-narrow text-center">
    <p class="footer-built">
      Built by <a href="http://www.joshhersh.com" target="_blank" rel="noopener">Josh Hersh</a> in San Francisco
    </p>
    <p class="footer-inspired">
      Inspired by friendships that deserve better than Instagram
    </p>

    <div class="footer-contact">
      <p>Questions? <a href="mailto:help@kithandkinapp.com">help@kithandkinapp.com</a></p>
    </div>

    <p class="footer-copyright">
      &copy; {new Date().getFullYear()} Kith & Kin
    </p>
  </div>
</footer>

<style>
  .footer {
    padding: var(--space-2xl) 0;
    border-top: 1px solid var(--border);
  }

  .footer-built {
    font-size: 1rem;
    color: var(--text-dark);
    margin-bottom: var(--space-xs);
  }

  .footer-built a {
    color: var(--brown);
    font-weight: 500;
  }

  .footer-inspired {
    font-family: var(--font-serif);
    font-style: italic;
    color: var(--text-muted);
    margin-bottom: var(--space-lg);
  }

  .footer-contact {
    margin-bottom: var(--space-md);
  }

  .footer-contact p {
    font-size: 0.9375rem;
  }

  .footer-contact a {
    color: var(--sage);
    font-weight: 500;
  }

  .footer-social {
    display: flex;
    justify-content: center;
    gap: var(--space-md);
    margin-bottom: var(--space-lg);
  }

  .footer-social a {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    color: var(--text-muted);
    background-color: var(--sage-light);
    border-radius: 50%;
    transition: all 0.2s ease;
  }

  .footer-social a:hover {
    background-color: var(--sage);
    color: var(--white);
  }

  .footer-copyright {
    font-size: 0.8125rem;
    color: var(--text-muted);
  }
</style>
