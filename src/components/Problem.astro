---
---

<section class="problem">
  <div class="container-narrow text-center">
    <h2 class="reveal">Social media companies promised us connection.</h2>
    <p class="problem-line reveal reveal-delay-1">Instead, we got rage, toxic comparison, and shattered attention spans.</p>
    <h3 class="problem-statement reveal reveal-delay-2">Shareholders have always been at the center. Not us.</h3>
      </div>
</section>

<style>
  .problem {
    background-color: var(--white);
    padding: var(--space-3xl) 0;
  }

  .problem h2 {
    margin-bottom: var(--space-sm);
  }

  .problem-line {
    font-size: 1.25rem;
    color: var(--text-muted);
  }

  .problem-statement {
    font-size: 2rem;
    color: var(--brown-dark);
    margin-top: var(--space-lg);
  }

  .problem-break {
    width: 60px;
    height: 2px;
    background-color: var(--brown-light);
    margin: var(--space-xl) auto;
  }

  .problem-want {
    font-family: var(--font-serif);
    font-size: 1.5rem;
    font-style: italic;
    color: var(--text-dark);
    line-height: 1.6;
    margin-bottom: var(--space-lg);
  }

  .problem-question {
    font-family: var(--font-serif);
    font-size: 1.375rem;
    font-weight: 600;
    color: var(--sage);
  }

  @media (max-width: 768px) {
    .problem-want {
      font-size: 1.25rem;
    }

    .problem-want br {
      display: none;
    }
  }
</style>
