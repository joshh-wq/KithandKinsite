---
---

<section class="professionals">
  <div class="container-narrow">
    <div class="professionals-card">
      <span class="professionals-label">For Mental Health Professionals</span>

      <blockquote class="professionals-quote">
        "I want to quit Instagram, but I'm afraid I'll lose touch with friends."
      </blockquote>

      <p class="professionals-familiar">Sound familiar?</p>

      <p class="professionals-text">
        Kith & Kin gives you something concrete to recommend:
      </p>

      <ul class="professionals-list">
        <li>Time boundaries built in</li>
        <li>No comparison metrics</li>
        <li>Private inner circle only</li>
        <li>Four ways to build relationship depth</li>
      </ul>

      <a href="#" class="btn btn-outline">
        Resources for professionals
        <span>&rarr;</span>
      </a>
    </div>
  </div>
</section>

<style>
  .professionals {
    padding: var(--space-3xl) 0;
  }

  .professionals-card {
    background-color: var(--white);
    border-radius: 1.5rem;
    padding: var(--space-2xl);
    text-align: center;
    box-shadow: 0 4px 20px rgba(31, 41, 33, 0.06);
  }

  .professionals-label {
    display: inline-block;
    background-color: var(--sage-light);
    color: var(--sage);
    font-size: 0.8125rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    padding: 0.5rem 1rem;
    border-radius: 2rem;
    margin-bottom: var(--space-lg);
  }

  .professionals-quote {
    font-family: var(--font-serif);
    font-size: 1.625rem;
    font-style: italic;
    color: var(--text-dark);
    line-height: 1.4;
    margin-bottom: var(--space-sm);
    border: none;
    padding: 0;
  }

  .professionals-familiar {
    font-size: 1.125rem;
    color: var(--text-muted);
    margin-bottom: var(--space-lg);
  }

  .professionals-text {
    font-size: 1.125rem;
    color: var(--text-dark);
    margin-bottom: var(--space-md);
  }

  .professionals-list {
    list-style: none;
    margin-bottom: var(--space-lg);
  }

  .professionals-list li {
    position: relative;
    padding-left: 1.75rem;
    margin-bottom: 0.625rem;
    color: var(--text-muted);
    text-align: left;
    max-width: 320px;
    margin-left: auto;
    margin-right: auto;
  }

  .professionals-list li::before {
    content: "";
    position: absolute;
    left: 0;
    top: 0.5rem;
    width: 8px;
    height: 8px;
    background-color: var(--sage);
    border-radius: 50%;
  }

  @media (max-width: 768px) {
    .professionals-card {
      padding: var(--space-lg);
    }

    .professionals-quote {
      font-size: 1.375rem;
    }
  }
</style>
